# ── PyTorch stack ────────────────────────────────────────────────────────────
# torchtune 0.6.1 requires torchdata==0.11.0, which was released with torch 2.6.
# The cu124 wheel index provides CUDA 12.4 builds compatible with sm_75+ GPUs
# (RTX 2000+, T4, A100, H100).
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchdata==0.11.0
torchtune==0.6.1

# ── Hugging Face ecosystem ───────────────────────────────────────────────────
transformers
safetensors
huggingface_hub[hf_transfer]
datasets

# ── Numerical / scientific ───────────────────────────────────────────────────
numpy
scipy
pandas

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib
plotly
seaborn

# ── Async HTTP ───────────────────────────────────────────────────────────────
aiohttp

# ── Experiment tracking ──────────────────────────────────────────────────────
wandb

# ── Anthropic API ────────────────────────────────────────────────────────────
anthropic

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm
omegaconf
ipython

# ── Note: sglang excluded ────────────────────────────────────────────────────
# sglang 0.5.9 pulls in flashinfer, torchao, cuda-python and other heavy
# CUDA-specific packages that conflict with the pinned torch 2.6.0 above.
# Install it separately in a dedicated inference environment if needed.
